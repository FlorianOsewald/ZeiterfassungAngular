
    class Settings {
        IsMaleAnrede: boolean;
        Name: string;
        State: ProgramState;

        ToString() {
            return 'isMaleAnrede:' + this.IsMaleAnrede + '\tName:' + this.Name;
        }
    }

